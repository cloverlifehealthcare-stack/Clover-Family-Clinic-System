export function ComingSoonPage({ title }) {
  return (
    <div>
      <h1>{title}</h1>
      <p>This screen isn't built yet — the API behind it already works and is fully tested.</p>
    </div>
  );
}
