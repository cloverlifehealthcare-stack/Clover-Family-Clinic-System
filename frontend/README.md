# Clover Clinic — Staff SPA

Not started yet. Comes after the backend Auth & RBAC foundation (`../backend`) has a working
login/permission API to build against — see `../docs/clover-architecture.md` §7 for build order.
